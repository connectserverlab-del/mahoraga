diff --git a/chrome/browser/mahoraga/core/BUILD.gn b/chrome/browser/mahoraga/core/BUILD.gn
new file mode 100644
index 0000000000000..fe42a921e2eef
--- /dev/null
+++ b/chrome/browser/mahoraga/core/BUILD.gn
@@ -0,0 +1,81 @@
+# Copyright 2024 The Chromium Authors
+# Use of this source code is governed by a BSD-style license that can be
+# found in the LICENSE file.
+
+# Mahoraga core constants and utilities.
+
+source_set("core") {
+  sources = [
+    "mahoraga_constants.h",
+    "mahoraga_switches.h",
+  ]
+
+  deps = [
+    "//base",
+  ]
+}
+
+source_set("product") {
+  sources = [
+    "mahoraga_product.cc",
+    "mahoraga_product.h",
+  ]
+
+  deps = [
+    ":core",
+    "//base",
+    "//chrome/browser/mahoraga:buildflags",
+  ]
+}
+
+source_set("prefs") {
+  sources = [
+    "mahoraga_prefs.cc",
+    "mahoraga_prefs.h",
+  ]
+
+  deps = [
+    ":core",
+    ":product",
+    "//base",
+    "//chrome/browser/ui/actions:actions_headers",
+    "//chrome/common:constants",
+    "//components/bookmarks/common",
+    "//components/pref_registry",
+    "//components/prefs",
+    "//ui/actions:actions_headers",
+  ]
+}
+
+source_set("unit_tests") {
+  testonly = true
+  sources = [ "mahoraga_prefs_unittest.cc" ]
+
+  deps = [
+    ":prefs",
+    ":product",
+    "//base/test:test_support",
+    "//chrome/browser/mahoraga:buildflags",
+    "//components/bookmarks/browser",
+    "//components/bookmarks/common",
+    "//components/pref_registry",
+    "//components/sync_preferences:test_support",
+    "//testing/gtest",
+  ]
+}
+
+source_set("action_utils") {
+  sources = [
+    "mahoraga_action_utils.h",
+  ]
+
+  deps = [
+    ":core",
+    "//base",
+    "//chrome/browser/ui:ui_features",
+    "//chrome/browser/ui/actions:actions_headers",
+    "//chrome/browser/ui/views/side_panel",
+    "//chrome/common:chrome_features",
+    "//ui/actions",
+  ]
+}
