# Generated by bun run env:examples. Do not edit by hand.
# This is the development env example for the Mahoraga monorepo root.
# Copy it to .env.development; real .env.development files are gitignored.

# --- dev-tools ---

# CDP protocol JSON input for browser protocol codegen.
# CDP_PROTOCOL_JSON=/path/to/chromium/src/out/Default_arm64/gen/third_party/blink/public/devtools_protocol/protocol.json

# Mahoraga binary used by app and Claw development.
MAHORAGA_BINARY=/Applications/Mahoraga.app/Contents/MacOS/Mahoraga

# --- app ---

# Chromium remote debugging port for app development.
MAHORAGA_CDP_PORT=9005

# Mahoraga server port, also used as the Claw browser launch port.
MAHORAGA_SERVER_PORT=9105

# Extension dev server port for app development.
MAHORAGA_EXTENSION_PORT=9305

# Browser bundle PostHog key.
VITE_PUBLIC_POSTHOG_KEY=

# Browser bundle PostHog host.
VITE_PUBLIC_POSTHOG_HOST=

# Browser bundle Sentry DSN.
VITE_PUBLIC_SENTRY_DSN=

# Public Mahoraga API URL exposed to the browser bundle.
VITE_PUBLIC_MAHORAGA_API=https://api.mahoraga.com

# Alpha feature flag for the browser bundle.
VITE_ALPHA_FEATURES=true

# Optional GraphQL schema path; falls back to schema/schema.graphql.
GRAPHQL_SCHEMA_PATH=

# Sentry auth token for source-map uploads.
SENTRY_AUTH_TOKEN=

# Sentry organization for source-map uploads.
SENTRY_ORG=

# Sentry project for source-map uploads.
SENTRY_PROJECT=

# --- claw ---

# Optional Claw API base URL override; tools/dev injects this in dev:claw flows and real env wins.
# VITE_MAHORAGA_CLAW_API_URL=http://127.0.0.1:9200

# Optional Chromium user data directory override for Claw dev browser launches.
# MAHORAGA_USER_DATA_DIR=/tmp/my-mahoraga-dev

# Optional Chromium remote debugging port override shared by Claw app and server dev.
# MAHORAGA_CLAW_CDP_PORT=49337

# Optional BrowserClaw state root override.
# BROWSERCLAW_DIR=~/.browserclaw-dev

# Claw server PostHog project key required by production builds.
CLAW_POSTHOG_KEY=

# Optional Claw server PostHog host; defaults to PostHog US Cloud.
# CLAW_POSTHOG_HOST=https://us.i.posthog.com

# BrowserClaw PostHog project key embedded in the shipped client bundle; required by production builds.
VITE_CLAW_POSTHOG_KEY=

# Optional BrowserClaw bundle PostHog host; defaults to PostHog US Cloud.
# VITE_CLAW_POSTHOG_HOST=https://us.i.posthog.com

# --- server ---

# Mahoraga server config URL required by production builds.
MAHORAGA_CONFIG_URL=https://llm.mahoraga.com/api/mahoraga-server/config

# Trusted origins for local server development.
MAHORAGA_TRUSTED_ORIGINS=

# Server telemetry key; CLI release builds read the same key.
POSTHOG_API_KEY=

# Server Sentry DSN.
SENTRY_DSN=

# Node environment for server and build scripts.
NODE_ENV=development

# Server log level.
LOG_LEVEL=info

# Optional AI SDK DevTools capture toggle for local server runs.
# MAHORAGA_AI_SDK_DEVTOOLS=true

# Headless browser setting for local server tests.
MAHORAGA_TEST_HEADLESS=true
